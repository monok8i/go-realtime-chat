// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: query.sql

package gen

import (
	"context"
)

const createMessage = `-- name: CreateMessage :one
INSERT INTO messages (user_id, chat_id, text)
VALUES ($1, $2, $3)
RETURNING id, user_id, chat_id, text, created_at
`

type CreateMessageParams struct {
	UserID int32
	ChatID string
	Text   string
}

func (q *Queries) CreateMessage(ctx context.Context, arg CreateMessageParams) (Message, error) {
	row := q.db.QueryRow(ctx, createMessage, arg.UserID, arg.ChatID, arg.Text)
	var i Message
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.ChatID,
		&i.Text,
		&i.CreatedAt,
	)
	return i, err
}

const getMessagesByChat = `-- name: GetMessagesByChat :many
SELECT id, user_id, chat_id, text, created_at FROM messages
WHERE chat_id = $1
ORDER BY id
`

func (q *Queries) GetMessagesByChat(ctx context.Context, chatID string) ([]Message, error) {
	rows, err := q.db.Query(ctx, getMessagesByChat, chatID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Message
	for rows.Next() {
		var i Message
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.ChatID,
			&i.Text,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
